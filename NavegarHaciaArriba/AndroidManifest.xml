<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jarroba.navegarhaciaarriba"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Libros"
            android:label="@string/app_name" android:launchMode="singleInstance" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LibroA"
            android:parentActivityName=".Libros" android:launchMode="singleInstance" >
        </activity>
        <activity
            android:name=".Videos" android:launchMode="singleInstance" >
        </activity>
        <activity
            android:name=".VideoA"
            android:parentActivityName=".Videos" android:launchMode="singleInstance" >
        </activity>
    </application>

</manifest>